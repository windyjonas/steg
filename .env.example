# Path to the JSON cache file (writable by web server user, e.g. www-data)
CACHE_FILE=/var/lib/garmin-cache/steps.json

# Garmin OAuth tokens directory (created by garmin_setup.py)
GARMIN_TOKEN_DIR=/root/.garth

# Daily step goal
GOAL_PER_DAY=10000
